rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DIAGNOSTIC ONLY: Open rules to test if they are being applied
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
